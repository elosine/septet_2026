# PROJECT PLANNER — septet 2026

> **What this is** (the tuba piece's device, kept): the working view of the PIECE as a
> collapsible outline — SECTION → container → gesture → decisions. Raw ideas land in
> `COMPOSITION_NOTES.md` verbatim first and get folded into a tier here. Engineering
> detail stays in `PLAN.md`. Rewritten freely; the sketch pad is the append-only record.

**NOW ►** 2026-09-08 — **PLAN 1l BUILT (RUNNING_LOG §262; docs/CRESCENDO.md): the crescendo object (`cresc.js` — his three families on his own ratio ladder, the 0.17 s end and the 5 s fallback, the cliff, the morph orange, "no room" as an answer), the LISTENING TEST as a score file (`scores/cresc-test.json`: 63 crescendos, 3 shapes × 3 durations on all seven lanes at the same times — solo a part to swap the instrument), and the ONE SPACING RULE (`spacing.js`: a player is free 150 ms after its last sound ENDS, with the GESTURE CLAUSE the measurement forced; the chords drawer re-read; the trill's fallback 3 s). 43 checks; the three shapes proved to reach the ear differently through the measured law. THE STANDARD IS NAMED (§263: surge is the default, line and bloom stay as options, bloom revisited at first use) and **PLAN 1l gained STEP 5 — SECCO AND THE ROUND ROBIN** (CN-49 · CN-50; §263–266): a crescendo is secco by default (the notation text, the strings' damp, a CC7 cut so nothing rings), and because this rack pins CC7 = 127 before every event on one slot per instrument, crescendos ROTATE through a small pool of slots in the back end — the napkin says three suffice above ~0.75 s a crescendo (a fourth only for the sub-second case), the strings and the bass clarinet have free channels inside the instance they already have, and the flute's port is full (its exception is the second UVI instance or one new port). STEP 5 BUILT to the edge of the rack (§267): the CUT (CC7 0 ten ms before the note-off, with its guard; verified by the decoded MIDI), the TOLERANCE PROBE as a score file (`scores/cresc-secco-test.json` — five gaps, every player, he solos and hears where the tail returns), and the POOL AND ITS ROTATION (`Cresc.assignSlots`, the tick sending on the assigned slot, a WARNING when three cannot keep the rest); 50 checks; the pool is empty until the slots exist, so nothing changes yet. ► next: THE RACK — two more Kontakt slots per string and for the bass clarinet (channels 2 · 3 · 4 are free inside each existing instance; the FLUTE's port is full, its pool being the second UVI instance or one new port), tried through the Kontakt Lua API and otherwise five minutes of his GUI time; then his two listening files; then 1m (the C key) is planned. Also waiting: his tests of 1k, 1j and 1i.** Before that — **THE CRESCENDOS IN PLANNING (RUNNING_LOG §252–257; CN-48): his three builds organized as FOUR items at his word — 1l the crescendo itself (the foundation), then 1m the C key, 1n the sequence filler, 1o crescendo strikes. PLAN 1l's phase 1 is complete and its top line confirmed (1 the listening test · 2 the crescendo object · 3 the spacing rule · 4 verify and document): the SURVEY read from his own pieces (bloom · surge · linear; the tuba standard surge 5×, already in this app's presets by name), a crescendo IS a held note whose curve rises (edited the curve-lane way — the line dragged, no diamond — and drawn filled and transparent in the morph ORANGE), the peak a CLIFF, 0.17 s before the next note for trills and crescendos alike, the fallbacks 3 s and 5 s, and ONE spacing rule everywhere: a player is free 150 ms after its last sound ENDS. The listening test is a SCORE FILE at his ask — the grid on all seven lanes at the same times, so soloing swaps the instrument. PLAN 1l is PLANNED WHOLE (§261): 1 the listening test as a score file · 2 the crescendo object · 3 the spacing rule with its GESTURE CLAUSE (the rest applies between gestures, never inside one — the measurement of his piece showed the plain rule would have outlawed 18 re-breaths of the BLOOM, §260) · 4 verify and document. ► next: THE BUILD at his word, step 1 first. **THE ORDER AFTER IT, his and written into the plan as stubs so it survives any clear: 1m (the C key) → 1n (the sequence filler) → 1o (crescendo strikes), each PLANNED when its turn comes, then built.** Also waiting on him: the tests of 1k (chords mode), 1j (the piano's lines) and 1i (the piano's harmonics).** Before that — **PLAN 1k BUILT (RUNNING_LOG §248–251; CN-44 … CN-47): CHORDS MODE in the strikes drawer — the same rhythm with a chord, or part of one, on every onset, dealt over the players by rule (the chord list with its order and advance, the selection from the drawer's voicing vocabulary, a range of players per onset lowered inside the range when too few are free and flagged only at the minimum, the notes by register folded into each range, no player striking again within 200 ms); the screen his own screen with a CURSOR (the keyboard shows one onset, the rhythm strip is the selector); a card at any onset to set it by hand; a span marked by a drag or SHIFT-click so Hear and both inserts write that PART, in notes mode too; a take carrying the RESULT and the RECIPE, loaded *as it was* or *as settings*. `strike_chords.js` (pure, 35 checks) · `strike_chords_ui.js` (a mixin) · five dispatch lines in the drawer; walked on a copy with real events and the decoded MIDI, three defects fixed on the walk; STRIKES_TOOL §X. ► next: HIS TEST — a hard reload, the drawer, chords, a chord list, Generate, Hear, a span, insert; and his tests of PLAN 1j (the piano's lines) and 1i (the piano's harmonics), both still pending.** Before that — **CN-44 IN DISCUSSION (RUNNING_LOG §234; the planning method, phase 1): the strikes drawer augmented — the rhythm's onsets carrying CHORDS or parts of one, a player count per onset, the dealer spreading the pitches, no player striking again within 200 ms; the data read (the drawer's one-note-per-onset model, the run's round robin / free dealer / re-deal / re-attack box, the 99 stored chords, the ensemble's 35 attacks a second at 200 ms); topic 1 ANSWERED by widening it (§235; CN-45): MENUS the machine solves — a range of players per onset, a menu of chords with an order (in turn · shuffled) and a dwell (exhaust · n times each), and a third ask: inserting PART of the finished sequence; topic 2 ANSWERED — all three (§236; CN-46): the SELECTION is the drawer's voicing menu (as played · shuffle · high / low cluster), the ADVANCE is separate (exhaust · n times · a fresh chord every onset), and any single onset may be set BY HAND with the collision still checked; topic 3 DECIDED (§237, "a": the count lowered inside his range, flagged only at the minimum; a manual onset flagged, never lowered); topic 4 DECIDED (§238, "a": a card at the onset); topic 5 DECIDED (§239, "c"); topic 6 DECIDED (§240) — PHASE 1 COMPLETE, PHASE 2 CONFIRMED, PLAN 1k OPENED with its six steps (1 the chord engine · 2 chords mode in the drawer · 3 the manual onset · 4 the span and the partial insert · 5 saving · 6 verify and document); PHASE 3 in progress, his format: each step's goal and sub-steps in one turn. steps 1 and 2 agreed and written (§241–243), the screen answered (§242: the keyboard shows ONE onset, the rhythm strip is the selector, his double-click becomes the manual onset). ALL SIX STEPS agreed and written — PLAN 1k PLANNED WHOLE (§247), shown to him once. ► next: his answer, then THE BUILD at his word — step 1 (the chord engine) first, each step a chunk on a copy (proposal → go → build → verify → docs → commit → push). Pending his hands: the test of PLAN 1j and the listening of 1i's piano harmonics.** Before that — **PLAN 1j BUILT (RUNNING_LOG §229–233; CN-43): the piano's ARTICULATION POINTS from a morph — *lines → piano* on the morph panel (every note's onset · peak · end as empty notes on the piano lane, in the morph's group; a re-run keeps the notes made), the lines drawn thin in the player's colour with a head by kind, the BAR at the left end of the piano lane (the kind ticks, the player ticks — kind AND player — clear lines; the browser's setting), THE PICKER CARD on a click (the vertical keyboard with the ensemble at that instant, the four voices, ppp … fff, the duration presets and box, ▶ in context, a key click that sets and sounds, ENTER / ESC; a made note reopens); `piano_cues.js` · `cue_picker.js` · the CLI · 32 checks; walked on a copy with real events and the decoded MIDI, two defects fixed on the walk. ► next: HIS TEST — a hard reload, his test score, [Morph], the playhead in the BLOOM, lines → piano, the ticks, a line clicked, a key, a voice, a dynamic, a duration, ENTER, Play; his verdicts to MORPH_NOTES §3 and NITS; and his listening of the piano harmonics (§218).** Before that — **CN-43 IN DISCUSSION (RUNNING_LOG §219): the piano's part on ARTICULATION POINTS generated from the morph — empty lines at the re-breaths, the apexes and the breath ends, each clicked into a note (a vertical keyboard, the four piano voices, ppp … fff, a duration); the planning method's phase 1 — topic 1 DECIDED (§220: *a line is an empty note*); topic 2 DECIDED (§221: the card at the line, a key click sets and sounds); topic 3 DECIDED (§222: the bar on the piano lane); PHASE 1 COMPLETE; PHASE 2 CONFIRMED (§223) — PLAN 1j opened with the five steps (1 the line · 2 lines → piano · 3 the picker card · 4 the bar · 5 verify and document); PHASE 3 in progress: step 1 (the line) agreed and written with its sub-steps (§224); his format — each step's goal and sub-steps together. step 2 (lines → piano) agreed and written (§225). step 3 (the picker card) agreed and written (§226). step 4 (the bar) agreed — the rule kind AND player (§227) — and written. step 5 agreed and written (§228) — PLAN 1j PLANNED WHOLE, shown to him once. ► next: his "is that it?" answer, then THE BUILD at his word — step 1 (the line) first, each step a chunk on a copy (proposal → go → build → verify → docs → commit → push).** Before that — **PLAN 1i FIRST PASS BUILT (RUNNING_LOG §217–218; CN-40): ♪ piano harmonics on the morph panel — at his "good go please", one piano note per re-breath of the morph under the playhead (the player's pitch at the re-breath → the nearest key whose sampled octave harmonic sounds it; the switch *at the pitch* · *an octave above*, the default at the pitch from his "detuned from what's being played"; the level box, 7), the notes in the morph's group with their provenance (NAMING §2.12), replaced by a re-run, undone by CTRL+Z; `piano_harmonics.js` (pure) + the CLI + 38 checks; verified on a copy with real events and the decoded MIDI (channel 3; the three opening harmonics C#3 · G#1 · G2 at velocity 109); two NITS (the harmonics loudness law is the Steinway's; the placed morph's level scale). ► next: HIS LISTENING — a hard reload, his test score, [Morph], the playhead inside the BLOOM at 183 s, ♪ piano harmonics, Play; the switch and the level by ear; then PLAN 1i item 2 (the CC21 shift: the chain, an exact detune) at his word.** Before that — **CHECKPOINT before a clear, 2026-09-08 early (journal §2):** the composer composes SECTION 2 from 183 s with the morph panel (the cast, the pitch menu, the actuals with recall), Name version / Reload / Restore… for his tries; the beating drawer parked; **► next at his word: PLAN 1i — the piano's HARMONICS at a morph's re-breaths (CN-40): read the actual's notes and the two-piano piece's IRCAM harmonics plan, restate the model in one line, ask which morph and whether the notes go in now on a stand-in voice, then build the generator on a copy.** Before that — **RESTORE BUILT (RUNNING_LOG §216; D27): Restore… beside Reload makes a named version the piece file again, the current file frozen first as v<next>-before-restore; verified on copies with real events; his 26 named versions and the piece file committed (7 MB, no audio) — the score files join the takes and the actuals at every wrap. His section-2 tries at 183 s: Name version at the start, at each try, at each keeper; Reload = throw away; Restore = back to that one.** Before that — **branching tries (RUNNING_LOG §215): a RESTORE (a named version made the piece file again, the current file frozen first) proposed for his tries at 183 s — to build at his word; until then the AI swaps files on request. CN-42: Lake George a rondo with the morph as the refrain.** Before that — **SECTION 2 BEGINS AT 183 s (CN-41; RUNNING_LOG §214):** he tries things in the main score and reverts if disliked — the recipe given (Name version 1.25-sec2start first · Save only at a keeper · Reload = back to the last Save · Name version at each keeper); the named versions' commit at every wrap put to him. Before that — **RECALL → MODELS built on every ACTUALs card (RUNNING_LOG §213), and a defect fixed on the way: the server rendered every actual without the septet palette — the save path, the validator and a --rebuild fixed, his seven actuals rebuilt (senza vibrato and ord, the cast's lanes), three dangling store references dropped; 64 checks; verified on a copy (the recall exact and stable through the poll).** Before that — **the ending answered (the release box = the run-down: the pitches close, the level fades; duration = the body) and the actual's default label with its number, length and pitch source (RUNNING_LOG §212).** Before that — **the root box fixed (RUNNING_LOG §211): it reaches SPECTRAL's fundamental with any source, and a bare pitch class ("D#") reads, the octave the model's; 58 checks; verified on a copy.** Before that — **a composition note (CN-40; RUNNING_LOG §210): the piano's HARMONICS at the morph's re-breaths — the nearest strong one, a little detuned; two ways (the MIDI from the render; the IRCAM harmonics faked as in the two-piano piece, its plan found) — PLAN 1i opened as todo, for when the morph section is composed.** Before that — **his first ACTUAL (ACT-SPACING-01) and his takes committed at his word; the rule: takes and actuals at every wrap (RUNNING_LOG §209; HOW_WE_WORK).** Before that — **THE PITCH SOURCE BUILT (RUNNING_LOG §208):** the PITCHES row in the morph panel — the sonority pull-down in eight groups (the model's set · kept · starters · the models' sets · stacks · Messiaen's modes · strikes · blasts · chord shapes, notes spelled), the root box (SPECTRAL's fundamental and the families' transposition), the take rule (by register the default · lowest · highest · spread · from k · every other · random), one or two per pair, keep / ✕ through the takes API; `bank/morph_pitches.json` the five starters (CN-38); 56 checks pass; verified on a copy with real events. **► next: HIS LISTENING — a hard reload, [Morph], MODELS, BLOOM, Generate, Play; a starter from the menu; a pair alone; Insert; CONVERGE; a seat swapped.** Before that — **the selection strategies (RUNNING_LOG §207):** a 'take' pull-down for a sonority wider than three — lowest · highest · outer + middle · spread · by register (recommended default) · consecutive from k · every other · random (seed) · two per pair (adjacent or spread); the modes' question of §206 dissolves into it; **► next: his choice of strategies and the default, then the menu built in one pass.** Before that — **the pitch menu's design grown (RUNNING_LOG §206):** no keyboard, the note names as text (the entries spell their notes; a line under the menu; the PAIRS lines); a `root` box for SPECTRAL's fundamental and for every generated family's transposition; the groups — the model's set · kept · the models' sets (the septet's and the tuba's) · the harmony list · stacks from the root (5ths, 4ths, M3, m3, whole tones, semitones; three doubled) · Messiaen's seven modes from the root; **one question to him: a mode as six degrees, three doubled, or both (recommended both)**; CN-39 the piano's solo scattered strikes parked in PLAN; **► next: his answer and his go, then the menu built in one pass; his listening of §204.** Before that — **the pitch discussion (RUNNING_LOG §205; CN-38):** five candidate sets from the harmony list (cs-015 · cs-019 · cs-021 · cs-050 · S055); no menu existed (the tuba way); a `pitches` pull-down proposed — the model's set · the kept sets by name · the harmony list in its groups; [keep] into the takes file's `morphPitches` bucket (no server change); three notes → one per pair, doubled; **► next: his go on the menu (then built in one pass, the five pre-kept), and his listening of §204.** Before that — **THE MORPH PANEL FOR THE SEPTET, BUILT (RUNNING_LOG §204):** the palette per voice into the pure engine (the tuba's renders byte-identical, `tools/morph_tuba_baseline.json`), the quartet's key rule and the 5 ms re-key overlap, the fold as one unit (D26), the emitter and the tick per instrument (the switch, the measured bend range, the remap), the six models re-voiced for three pairs (`bank/morph_models.json` rev 9), the PAIRS lines (a tick, two seat pull-downs, the pitches as folded) under the fields, the box 500 px at 13 px, the META shape on layer 7; `score/public/morph_septet.js` new; 38 checks (`tools/morph_septet_check.js`) all pass; verified on a copy with real events (the audition's CC0 / CC7 / bend per port, the swap, the ticks, an insert of one pair). **► next: HIS LISTENING — a hard reload (CTRL+SHIFT+R), [Morph], MODELS, BLOOM, Generate, Play; a pair alone; Insert; CONVERGE; a seat swapped by ear; then pitch sets by his word.** Before that — **THE MORPH PANEL BUILD, begun (RUNNING_LOG §203):** the Kontakt dial dropped at his word ("not worth my time. unless it's a blocker") — option (i): the engine splits by the real excursion at the measured ranges (the quartet's key rule), no probe; the two rack items in NITS. **The AI builds alone on a copy:** the engine's palette per voice · the split rule · the fold as one unit (D26) · the emitter and the tick per instrument · the six models re-voiced for three pairs · the pair lines with seat pull-downs and insert ticks · the box grown with its fonts · node checks · the decoded MIDI on a copy · MORPH_NOTES §1 · commit, push. **► next: the build; then HIS LISTENING — BLOOM on the default pairs first.** Before that — **the bend range in Kontakt (RUNNING_LOG §202):** the Lua API has no modulators (checked against the manual), the Xsample manuals say nothing, two candidates given him — the Xsample panel's `ind. PB` (test with the wheel) or Kontakt's Source → Modulation → Pitch Bend intensity 2.00 st with Edit All Groups — then save the Reaper project; the probe verifies; **his ask for the build: the morph panel's box grows with its fonts**; a rack note in NITS (the Xsample instruments do not accept All Notes Off). **► next: his dial, then the probe and the build.** Before that — **DECIDED (RUNNING_LOG §201): Kontakt to ±2 st on the five, the probe re-run, then THE MORPH PANEL ADAPTED** — the pitches the old way (he asks, the AI types the set); **his one step: the Kontakt dial on the five Xsample instruments, the rack left armed (Reaper + the REC track, the bridge, nothing else sending MIDI); then the AI alone:** `bash tools/probe_run.sh probes/bend_schedule.json` → `node tools/apply_bend_ranges.js` (the recipe's MEASURED_BEND, BEATING_TOOL §3, the log) → the morph panel on a copy in one pass (the palette per voice into the pure engine — the ordinary voice, the range, the bend reach, the breath / bow ceilings; the split by the real excursion with a 5 ms overlap; the fold as one unit, D26; the emitter and the tick per instrument; the six models re-voiced for three pairs; three pair lines with seat pull-downs and insert ticks; fonts a step up) → node checks + the decoded MIDI in the pane + real-event walks → MORPH_NOTES §1 → commit, push; **► next: his Kontakt dial and his word, then the run and the build; after it his listening (BLOOM on the default pairs first).** Before that — **one more pass (RUNNING_LOG §200):** the fold decided — (a) the pair folds as one unit (D26); the scratch file explained and the tuba's pitch choice traced (numbers typed by the AI at his word, the stock sets from the day-10 audit); the quartet's glissando method confirmed (the key one semitone into the travel, the bend from the far end, a segment per two semitones with a 5 ms overlap) against the engine (the key centred on the excursion, but a split rule far too eager at ±1 st) and the measured ranges (flute 2.00 · BCl 0.98 · Vn1 0.96 · Vn2 0.97 · Va 0.99 · Vc 0.97 st); put to him: (i) split by the actual excursion, a few cents clamped · (ii) Kontakt at ±2 st on the five and the probe re-run — recommended; **► next: his choice (i) / (ii) and his go, then the build in one pass on a copy.** Before that — **the morph panel discussed on (RUNNING_LOG §199):** his three points — the strings' ordinary voice senza vibrato (as the recipes say) · players SWAP among the pairs (a pull-down per seat, the pitches stay with the pairs, the render recast) · a whole tone within every player's reach (accepted; the sampler's one re-key per voice named as a mock-up seam) — and the OCTAVE DISPLACEMENT put to him as two models: (a) the pair folds as one unit (recommended, §180) · (b) only the player who cannot reach folds, the pair an octave apart; **► next: his answer on the fold and anything more to discuss, then the build in one pass on a copy.** Before that — **"keep it simple for now" (RUNNING_LOG §198):** the §197 proposal declined; the beating drawer LEFT AS IT IS (parked — its list in NITS for the rebuild session); the morph panel the TUBA WAY with two changes — the septet's six players (the piano out) with their palette under the hood, and the texture split into its pairs at Play / Insert (a tick per pair) — the picture put to him; **► next: his confirmation of the picture, then the build in one pass on a copy.** Before that — **THE MORPH PANEL, back at his word (RUNNING_LOG §197; CN-37):** the port's morph tool read against his six asks (six players minus the piano; three default pairs Vn1+Vn2 · Vc+Va · Fl+BCl; re-orchestrable; saved; the beating drawer's fold; a pair or any combination inserted) — found: the tuba's constants everywhere (lanes 0–7, the technique table, the breath table, the 1.99 st bend in three places, the ord route, the tuba cc7 map), the floating box, no player or pitch choice, ACTUALs frozen; the proposal put to him (the pair as the orchestration unit, the palette per voice into the pure engine, the emitter and the tick per instrument, a drawer on the beating drawer's chassis, a tick per pair, takes in a morphs bucket) with one question (three keeper models or six). **► next: his answer, then the build in one pass on a copy.** Earlier (RUNNING_LOG §196) — **his word: keep collecting the feature requests, a REBUILD SESSION later**; the §193 endpoint model deferred to it (its one open detail in NITS); the strikes drawer's partial insert (solo + insert) and its dynamic (dyn × on the played velocities; the five velocity bands) explained from the code; a per-player dynamic found missing → NITS. Earlier — 2026-09-07, night — **CHECKPOINT before a clear (journal §2; RUNNING_LOG §195):** the composer tests the beating drawer by hand and builds his first sequence; the lanes are the REGION MODEL of §188; the running list of issues and feature requests is `docs/NITS.md` (his words in MORPH_NOTES §3), the rules HOW_WE_WORK (save by default, one round per sitting, gestures as real sequences, a model agreed in one line); waiting: his next round, his yes to the §193 endpoint model, the stutter A/B. Earlier — **the column that jumped fixed (§189)** — a render kept the rows' scroll; the zoom's sensitivity and centre in NITS as feature requests at his word. Before that **THE REGION MODEL (§188), his DAW picture built in one pass after "this working process is not working":** the lanes on the sequence timeline, every node at its own time, the region's edges as cuts (a node beyond a cut waits), the score's zoom and scroll, a cursor while it plays, a gesture card, double-click and SHIFT-click to type a node (the double-click never fired: a click re-rendered the lane — fixed); HOW_WE_WORK: one round per sitting, gestures tested as real mouse sequences. **► next: his test of the region model; the first sequence in the piece.** Before that — **the picture of the sound (§187):** the beats themselves drawn and counted on the centre line, the hold shape typed in seconds, a node typed by double-click, one time scale for every row with a zoom, CTRL+Z reaching the drawer from anywhere — his *"I can't get the visual to look like the sound"*; before that **the lanes on a real time axis (§186):** the pair ends at its length, the end dot follows the mouse in seconds (his "I still can't move it"); before that **his first test of the sweep answered (§185):** the bend is the score's grab-and-pull control point (two degrees of freedom), the hold shape's attack and release are seconds (the sustain absorbs a length change), the end handle 1 s per 50 px, SHIFT clamps a node; his settings so far on record in §185 (cs-015, BCl + Fl on D4, 2.1 s, 8 Hz, hold, p → ff). **► next: his test goes on; the first sequence in the piece.** Earlier — **THE SWEEP BUILT (RUNNING_LOG §183–184; BEATING_TOOL §14):** the discussion he asked for was held — strategy A (his walk-through written back as a 23-line script, each line works · awkward · missing), rule B into HOW_WE_WORK (a panel borrows the score's idiom; a tool is planned against how he works), C held — and the misses built in one pass at his "go ahead": a pair born empty (double-click a note, click the node; both players pull-downs; the drag gone), the ADSR birth shape at 3 Hz over 9 s, max in Hz with ▶ max, his stored shapes, hold-the-line curve gestures (bend · shift · SHIFT-slide · wheel · ALT · double-click; the end handle = the length), a crescendo per player, ppp … fff over the whole measured scale, the breath lane simplified with unison, the timeline's span, auto-assign and revert on a harmony change, fourths ↔ fifths one family, mute / solo, undo (CTRL+Z), loop; two defects found on the walk and fixed. **► next: HIS TEST against the same script after a hard reload**, then the first beating sequence in the piece; the stutter A/B still his. Earlier, afternoon — **his first hour with the drawer: nine asks built in one pass (RUNNING_LOG §182; BEATING_TOOL §13)** — the sequence strip (a track per pair, dragged and stretched like the trill zones), a length and a play per pair, SPACE following the focus (sequence · pair · chord), the number boxes freed, the score's slope diamonds on the curves, the level box keeping a drawn shape (the "snaps back" he saw), pair takes, the crosshair; the stutter examined — the streams are as smooth as the string quartet's, **his A/B pending** (2/s · the crescendo flat · panel vs transport); the 0–1 level = the score's curve height. **► next: THE DISCUSSION he asked for** — *"a straightforward plan that will get this panel working quickly so I can get back to composing and hearing things … a reorg that will sweep up any blockers and individual details in one go"* — then his test of the drawer after a hard reload. Earlier, early afternoon — **the beating drawer's pitch side, second pass, BUILT at his word** (*"Q1 yes, Q2 b, Q3 yes, Q4 a … You can go ahead"*, RUNNING_LOG §179–181; BEATING_TOOL §12; his six asks in MORPH_NOTES §3): the voicing bar (the strikes presets, the octave box, the octave range, the seeded reshuffle with chips) · the pair folds a note as one unit to the nearest octave both reach, the arrows on both seats, a ladder of offers when no octave serves (never applied by itself), every player in the partner menu · the dotted lines from key to node · the players' ranges as columns on the keyboard · the octave interval · skip; 102 checks in node, verified with real DOM events on a copy. **► next: HIS TEST of it after a hard reload (CTRL+SHIFT+R)**, then his listening below. Earlier the same day (about 01:00, building PLAN 1f autonomously at his word — *"run the plan independantly … I'll check in after the build"*, RUNNING_LOG §167) — **1f step 1 (the palette) BUILT and its bend probe RUN in his rack** (§167–168; `docs/BEATING_TOOL.md` §3): SI2 ±2.00 st, the Xsample five ±0.96–0.99 st, RPN 0 ignored on all six, the residue real on all six, the pre-arm fine; `beating_calc.js` (the palette part) + its check; the numbers in the recipe as MEASURED_BEND. **1f step 2 (the beating math) BUILT** (§169; BEATING_TOOL §4): `renderPair` / `renderPattern` / `stretch`, the shapes, the mirror and the slide, the breaths dealt inside the ceilings, the re-key, the flags — 77 checks, nothing heard. **1f step 3 (one pair in the score) BUILT** (§170; BEATING_TOOL §5): the `beating` zone on two lanes, B on a strike note, the snippet with `_bend` events through `bendRangeSt`, the tick's centre after the end, the P row, ▶ hear — verified on a copy by the decoded MIDI (the beat rate within 0.002/s of the math, the just offsets at every interval, save / reload / drag / stretch / delete); **his ear pending** (unison, fifth, fourth). **1f step 4 (the panel) BUILT** (§171; BEATING_TOOL §6): `beating_panel.js` — bound to a selected beating by P / B or holding a pattern until Insert; the mirrored curves with handles on rails, the band by zone, shapes and draw, the mirror lock and ALT-drag, the slide, the crescendo and breath lanes (sliders, the ceiling, shuffle), the offset rail, the length box, SPACE, takes — verified with real DOM events on a copy; **his test pending**. **1f step 5 (the pitch side) BUILT** (§172; BEATING_TOOL §7): the strike menu from the bank, the keyboard beside the rows (the chord lit, the unplayable keys dimmed, the pairs' notes ringed), a note armed and clicked or dragged onto a pair, the relations dealt from a root and folded — verified with real DOM events on a copy; **his test pending**. **1f step 6 (insertion) BUILT** (§173; BEATING_TOOL §9): insert @ playhead from the panel — the beatings under one group with a META shape (the crescendo's mean), the shape's drag / stretch / delete carrying them (the three group paths learned zones), re-insert replacing at the same time, a second group elsewhere, select + P loading the group, nothing around it touched — verified on a copy; **his test pending**. **1f step 7 (verify and document) BUILT** (§174): the end-to-end on a copy within 0.006 beats/s of the panel on every pair, a defect fixed on the way (a player in two pairs), the help line (?), BEATING_TOOL complete, MORPH_NOTES §1 / §4, NAMING §2.10, D24 / D25 — **PLAN 1f BUILT, steps 1–7.** **His check-in, morning of 2026-09-07 (§175–177):** the panel made a full-page drawer with the fonts up · a trapped note — the cause fixed, ■ Panic · the harmonies on the left in three banners (the strikes · the tuba's blasts · the two-piano chord shapes, `bank/harmonies.json`), ▶ chord on the piano, a note armed by double-click and given to a pair's node, the harmony in the take (CN-36) · **CN-35: a harmony module** (the strikes drawer's facility over every collection) **noted in PLAN 1d for later — to be planned on his word.** **► next: HIS LISTENING** — a hard reload (CTRL+SHIFT+R), the steps 3–7 in the order of BEATING_TOOL §11 (B on a strike note; P; the fifth and the fourth; the panel's feel; the pitch side; the insertion), the verdicts to MORPH_NOTES §3 and BEATING_TOOL, the first pattern in the piece placed by him; then item 8's held things when their lines come, the trills' phase 5 and the run dials' calibration on his go. Earlier — 2026-09-06 (checkpoint, evening) — **section 1 is drafted: the piece to 175.7 s** in `scores/piece-septet.json` = his `v1.23-sec1DraftDone` (48 strikes, 69 trills; from 135.84 s the trills are minor 2nds, CN-32); **this session:** 1g done · 0d done · **1h built** (the acceleration calculator `score/public/accel_calc.js` + the drawer's run dials, the free dealer and the whole-strike pool, the even run; RUNNING_LOG §125–142) · the version suggestion, the All-trills attack batch, the free trill's 0.17 s end, the multi-selection group drag (§139–144); **PLAN 1f opened, evening (RUNNING_LOG §145–151): the object is a *beating* (a pair of players on one pitch, a gap that beats), the beating panel his picture, the axis beats per second, the piano out; the eight-line top line in PLAN 1f, steps 1–6 (the palette · the beating math · one pair in the score, heard · the panel · the pitch side · insertion — the beating out of the strike chain, §160) agreed and written; PLAN 1f planned whole and confirmed ("yes that's it", §166) — eight steps in the fixed format, nothing built; **next: build step 1, the palette (the bend probe — his rack window), and step 2, the beating math, while the probe waits; then 3 → 7 on a copy, his listening at each**, then the build of step 1 — the bend probe in his rack**, the run dials' calibration ladder on his go, phase 5 (the weave) when the trills ask. Phase 0 so far: 0a · 0b · 0d · 0e · 0g · 0i · 0j · 0k.1–0k.4 done; piccolo vs bass flute (CN-2) open.

## The piece — outline (v1 — CN-1 · CN-5 · CN-8 · CN-10)

**Duration cap:** 12:00 (the call). **Forces:** fl(picc/bfl) · bcl · pno · vn1 · vn2 · va · vc.

1. **OPENING — the scattered strikes, expanded** (CN-5 · CN-8 · CN-10) · the piano's 46 recorded strikes
   orchestrated one by one in the drawer (#0–#16 by 2026-09-04 night), then as ensemble processes:
   climbing scales through the ensemble · one accel/decel spread across the players (CN-3's ramps) ·
   antiphonal blocks from sub-ensembles · circular chord cycles as a Risset ladder through the registers
   (CN-7; CN-13: per-player tone rows completed over curves, performer jitter periodic → non-periodic) ·
   **the spreads' expansion** (CN-12): each strike's span wider than the last, anchored at its first impact.
   CN-1's single attack is one strike among them. **Next section @ 27.76 s** (CN-14): one strike re-struck,
   its spacing evened, each re-strike longer (his example ×1.5 from 100 ms), accelerating toward its last
   impulse once long enough to hear — to build at his word (STRIKES_TOOL V, SPREAD). Then straight into
2. **CURVE-BASED TREMOLOS** (the tool: `TRILLS_TOOL.md`, PLAN 1e — spec 2026-09-05) · each entry is a fortepiano (attacked, then continuing in
   tremolo); the tremolo shaped by a curve (open: which parameter the curve drives —
   speed, amplitude, pitch spread, or several); **the tremolo fork** (CN-11): the tremolos become
   their own branch, expanded like the strikes —
3. **TREMOLO FUGUE** · the tremolo material treated fugally (staggered entries across the
   seven voices — subject/answer relations to be defined) → **stretto** → accelerations and
   decelerations, singly or stacked (CN-3 layered) → **vibrato rate** speeding up and slowing down (CN-11)
4. **→ DENSITY-BUILD SOUND MASS** · the fugue resolves into a density build like the tuba
   piece (its recipes: `CURVE_DATABASE.md` MAXDENSE-1 / BUILD-1 in #4)
5. *(the rest: open)*

**CN-28 (2026-09-06) re-forms the middle** — *(AI reading, to confirm with him)*: **section 2 = strikes with morph chords**
("freeze frames / an old-time slide show": morph events launched from strikes, the tuba morph engine — PLAN 1f); **section 3 =
patterned or call-and-response strikes with crescendos** (STRIKES_TOOL V); the tremolos → fugue → mass of v1 after, or
interleaved — his to say.

## Open musical questions (for the composer, when the sandbox exists)

- What does "curve-based" govern in the tremolos? (the tuba piece's META curves were
  density/level targets — is the tremolo the same idea at the note level?)
- Which instrument carries the initial attack? (piano + Bartók pizz + slap tongue are the
  natural percussive attacks in these libraries; the tuba piece's "surge" was a swell)
- CN-10: which partition makes the sub-ensembles (strings / winds + piano · high / low · other)? And
  does the Risset ladder run on the recorded strikes' harmony or on CN-6's chosen harmony?
- Fugue: real pitch subject, or a textural "subject" (a tremolo shape) — the answer decides
  whether the sandbox needs a pitch-transform tool early.
